fix(deploy): repair AIO live-deployment blockers in compose, Dockerfile, bootstrap scripts

INTENT: FIX
CLASS: REPAIR
INVARIANT: Affects: Docker Compose schema v3 deploy.resources.limits, Dockerfile UID-1000 collision, Python tmpfs visibility, and bootstrap script service-enablement invariants for AIO 24/7 deployment.
DEGREE-OF-FREEDOM CLOSED: The agent could emit a Compose file with pids_limit at the service root (rejected by schema v3), create a container where useradd fails due to UID collision, or leave Python inaccessible because uv's ~/.local path is shadowed by tmpfs — all discovered only at runtime on the AIO host.
DETERMINISTIC MECHANISM: docker-compose.fa.yml:21 replaces the invalid top-level pids_limit: 512 with pids: 512 nested under deploy.resources.limits, Dockerfile.fa:29 removes any pre-existing UID-1000 user before useradd, Dockerfile.fa:48 symlinks the uv-installed Python into /usr/local/bin/python to survive the tmpfs mount, scripts/setup-fa-desktop.sh:108 enables and starts fail2ban, unattended-upgrades, and apt-daily-upgrade.timer after install, and scripts/fa-post-setup.sh:62 adds a Tailscale connectivity warning while scripts/fa-post-setup.sh:113 replaces the hardcoded git SSH URL with a fork-safe ${REPO_SSH_URL} derived from git remote get-url origin.

AI-Session: 829be595b4e74574b9849e6f0ef93f5b
Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
