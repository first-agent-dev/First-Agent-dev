name: Pylint

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.13"]
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        cache: pip
    - name: Install system dependencies
      # universal-ctags is a runtime system dep of the ADR-5 chunker; some
      # modules import-guard on tooling that benefits from it being present.
=======
>>>>>>> Stashed changes
      run: |
        sudo apt-get update
        sudo apt-get install -y universal-ctags
    - name: Install dependencies
<<<<<<< Updated upstream
      # Install the project itself plus dev extras (pytest, etc.) so pylint can
      # IMPORT every module it analyses. Without this, third-party imports
      # (markdown_it, jsonschema, yaml, bashlex, pytest) raise spurious
      # E0401 import-error and pylint also skips deeper analysis of those files.
=======
      # Install the project + dev extras so pylint can IMPORT every module.
      # Without this, third-party imports (markdown_it, jsonschema, yaml,
      # bashlex, pytest) raise spurious E0401 import-error.
>>>>>>> Stashed changes
      run: |
        python -m pip install --upgrade pip
        pip install -e ".[dev]"
        pip install pylint
<<<<<<< Updated upstream
    - name: Analysing the code with pylint
      # Lint config (disabled checks + fail-under threshold) lives in
      # pyproject.toml under [tool.pylint.*]. The job fails if the score drops
      # below `fail-under`, so real Error/Warning regressions still block,
      # while purely-cosmetic style nits do not.
=======
    - name: Lint src/ (strict; duplicate-code ENABLED)
      # Production code is held to the strict profile in [tool.pylint.*]
      # (pyproject.toml). duplicate-code (R0801) stays ON here because
      # copy-paste-and-tweak is the #1 LLM-agent code smell.
>>>>>>> Stashed changes
      run: |
        pylint $(git ls-files 'src/*.py' 'verifiers/*.py')
    - name: Lint tests/ (relaxed; duplicate-code DISABLED)
      # Tests use pytest idioms (fixtures shadow names, parallel scaffolding)
      # that are legitimately "duplicated"; relax via .pylintrc-tests.
      run: |
        pylint --rcfile=.pylintrc-tests $(git ls-files 'tests/*.py')
